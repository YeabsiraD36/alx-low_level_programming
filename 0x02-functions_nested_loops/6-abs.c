#include "main.h"
/**
 * _abs - function that puts integer in absoulute value
 * @n: test subject
 * Return: Void.
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
	return (n);
	}
}
